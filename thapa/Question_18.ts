//problem no:18
let famous_places=["Santorini","Kyoto","Machu Picchu",'Amalfi Coast',"Reykjavik"];
let sorted_list=[...famous_places];
sorted_list.sort();
console.log(famous_places); //Print your array in its original order.
console.log(sorted_list);//Print your array in alphabetical order without modifying the actual list.
console.log(famous_places);// Show that your array is still in its original order by printing it.
console.log(sorted_list.reverse());//Print your array in reverse alphabetical order without changing the order of the original list.
console.log(famous_places);// Show that your array is still in its original order by printing it again.
console.log(famous_places.reverse());// Reverse the order of your list. Print the array to show that its order has changed.
console.log(famous_places.reverse());//Reverse the order of your list again. Print the list to show it’s back to its original order.
famous_places.sort();//Sort your array so it’s stored in alphabetical order. 
console.log(famous_places);//Print the array to show that its order has been changed.
console.log(famous_places.reverse()); //Sort to change your array so it’s stored in reverse alphabetical order. Print the list to show that its order has changed.
